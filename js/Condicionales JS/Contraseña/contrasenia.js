const inicio = prompt("¿Desea cambiar su contraseña?");

const respuesta = "si" ;
const name = "Magdalena";
const date = 06
const hermana = "No"

let usuario = name && date && hermana

if(respuesta === "si") {
    alert("Queremos confirmar que es ud. ¿Está listo?");
    const preg1 = prompt("Escriba el segundo nombre de su madre");
}  if(name === "Magdalena") {
    const preg2 = Number (prompt("Ingrese el día de su nacimiento, usando 2 cifras"));
} if (date === 06) {
    const preg3 = prompt("Su hermana mayor, se llama Celeste?");
} if (hermana === "No") {
    alert("Ahora puede cambiar su contraña");
} if  (usuario) {
alert("Ud. no existe")

}